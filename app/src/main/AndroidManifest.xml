<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hawc.eecs582.com.hawc">

    <uses-permission
        android:name="android.permission.CAMERA2"
        android:required="true" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity
            android:name="GetPictureActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >

        </activity>

        <activity
            android:name=".Splash"
            android:label="@string/app_name">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".EditCodeActivity"
            android:label="Edit Code" >

        </activity>
        <activity
            android:name=".ViewOutputActivity"
            android:label="@string/title_activity_view_output" >
        </activity>
    </application>

</manifest>
